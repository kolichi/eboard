<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- View product.document kanban -->
  <record id="view_product_document_kanban" model="ir.ui.view">
    <field name="name">view.product.document.kanban</field>
    <field name="model">product.document</field>
    <field name="inherit_id" ref="product.product_document_kanban" />
    <field name="arch" type="xml">
      <xpath expr="//kanban"
        position="attributes"
      >
        <attribute name="js_class">product_documents_kanban_inherit</attribute>
      </xpath>
    </field>
  </record>

</odoo>